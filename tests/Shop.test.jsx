import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import Shop from "../src/sections/Shop";

describe("Shop", () => {
  it("renders ")

  //--mock child components--
  //renders ShopItem if allArr is not empty
  //don't render a <ShopItem /> if data type is wrong
  //don't render a <ShopItem /> if a field is missing
  //render "there are no products" if allArr is empty
  //does not render "there are no products" if there are <ShopItem /> components
})
